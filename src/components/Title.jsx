const Title = ({ pageTitle }) => {
  return (
    <div className="title-container">
      <div className="title-line"></div>
      <h1>{pageTitle}</h1>
      <div className="title-line"></div>
    </div>
  );
};

export default Title;
